{
  "permissions": {
    "allow": [
      "Exec(npm run)",
      "Exec(git status)",
      "Exec(git diff)",
      "Exec(git)",
      "Exec(npm)",
      "Exec(mkdir)",
      "Exec(node)",
      "Exec(python3)",
      "Exec(curl)",
      "Exec(npx tsc)"
    ]
  }
}